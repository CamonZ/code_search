//! Output formatting for reverse-trace command results.
//!
//! The output implementation is shared with the trace command in trace/output.rs.
//! Both commands use TraceResult with a direction field to determine rendering.
